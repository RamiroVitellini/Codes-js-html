let numbers = [2,3,4]

//console.log(numbers.length)

//console.log(Array.isArray(numbers))
/*
console.log(numbers)

let deleteFirstElement = numbers.shift(); //Elimina el primer elemento y lo devuelve
console.log(numbers)
console.log(deleteFirstElement) 

let deleteElement = numbers.pop() //Elimina el ultimo elemento y lo devuelve
console.log(numbers)
console.log(deleteElement)

let newNumber = numbers.push(6) //Añade un numero y devuelve la longitud
console.log(numbers)
console.log(newNumber)

let newLength2 = numbers.unshift(7,8) //Añade muchos numeros y devuelve la longitud
console.log(numbers)
console.log(newLength2)

console.log(numbers)
console.log(numbers.indexOf(8)); //Devuelve si esta o no el valor pedido


console.log(numbers);
let invertnumbers = numbers.reverse() //Da vuelta el orden del Array
console.log(invertnumbers)


console.log(numbers);
let arrayString = numbers.join('--') //Pone la separacion que quieras
console.log(arrayString)


console.log(numbers)
let deleteab = numbers.splice(2,3); //Elimina los elementos que pongas
console.log(numbers)


console.log(numbers)
let addab = numbers.splice(1, 0, 'a', 'b') //Añade elementos que pongas 
console.log(numbers)


console.log(numbers)
let remplaab = numbers.splice(1, 2, 'a', 'b') //Reemplaza elementos que pongas
console.log(numbers)


console.log(numbers)
let deleteadd = numbers.splice (1,1 ,'a', 'b') //Elimina y agrega
console.log(numbers)


console.log(numbers)
let newnum = numbers.slice(2,3) //Extrae elementos de a hasta b 
console.log(newnum)
*/

/*
let password = 'hola';
while(password != 'hola'){
    password = prompt('Introduzca su contraseña')
}
console.log('Fin del bucle');

do{
    pass = prompt('Introduzca su contraseña')

}while(pass != 'hola')


for(let i=0;i<numbers.length;i++){
    console.log(`i vale ${i} y el valor de esa posición en el array es ${numbers[i]}`);
}

let names = ['Paco', 'José', 'Paula', 'María']
for (let i = 0; i < names.length; i++){
    if(names[i]==='Paco'){
        continue
    }
    console.log(names[i]);
}

for(let index in names){
    if(names[index]==='Paula'){
        continue
    }
    console.log(index);
}

const person = {
    name: 'Juan',
    age: 26,
    sons: ['Laura', 'Diego', 'Pepe', 'Rosa', 'Tomás']
}

//Imprimir Atributos
for (const key in person){
console.log(key);
}

 //Imprimir lo que esta dentro del Atributo
for(const key in person){
console.log(person[key]);
}

//Imprimir dentro de la array
for(const sons in person.sons){
    console.log(person.sons[sons])
}

console.log(`Hola ${person.name}. tienes ${person.age} años y tus hijos se llaman ${person.sons.join (', ')}`);
*/




